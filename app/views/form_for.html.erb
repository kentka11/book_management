<%= form_for(@book) do |f| %>
  <div class="field">
  	<%= f.label :isbn %><br />
  	<%= f.text_field :isbn, size: 25 %>
  </div>
  <div class="field">
  	<%= f.label :title %><br />
  	<%= f.text_field :title, size: 25 %>
  </div>

  <div class="actions">
  	<%= f.submit %>
  </div>
<% end %>